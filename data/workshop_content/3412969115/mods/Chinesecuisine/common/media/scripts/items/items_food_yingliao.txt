module Chinesecuisine
{

		/****************************** 饮料物品. ******************************/

    item Anmuxi	/* 安慕希风味酸奶 */
	{
		DisplayName = Anmuxi,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.15,
		Icon = anmuxi,
		EatType = popcan,
		CustomContextMenu 	= Drink,
        DaysFresh        = 3,
        DaysTotallyRotten  = 3,
		StaticModel = anmuxi,
		WorldStaticModel = anmuxi,
		FillFromDispenserSound = GetWaterFromDispenserMetalMedium,
		FillFromLakeSound = GetWaterFromLakeSmall,
		FillFromTapSound = GetWaterFromTapMetalMedium,
		FillFromToiletSound = GetWaterFromToilet,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
            ContainerName   = carton,
			PickRandomFluid	= true,
		    Opened	= false,
            capacity        = 0.2,
            CustomDrinkSound = DrinkingFromCan,

            	Fluids
            		{
                	fluid           = anmuxi:1.0,
            		}
        	}
	}

		item AD_CalciumMilk	/* AD钙奶 */
	{
		DisplayName = AD CalciumMilk,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.1,
        Icon     = adgainai,
		EatType = popcan,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = adgainai,
		WorldStaticModel = adgainai,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
		    Opened	= false,
			capacity        = 0.22,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = ADCalciumMilk:1.0,
            		}
       		}
	}

	item IcedTea	/* 康师傅冰红茶 */
	{
		DisplayName = IcedTea,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = binghongcha,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = binghongcha,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = IcedTea:1.0,
            		}
       		}
	}

	item big_IcedTea	/* 康师傅大瓶冰红茶 */
	{
		DisplayName = big IcedTea,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.5,
        Icon     = dpbhc,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = dapingbinghongcha,
		WorldStaticModel = dapingbinghongcha,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = bigIcedTea:1.0,
            		}
       		}
	}

	item greentea	/* 康师傅绿茶 */
	{
		DisplayName = greentea,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = lvcha,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = lvcha,
		WorldStaticModel = lvcha,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = greentea:1.0,
            		}
       		}
	}

		item Sprite	/* 雪碧 */
	{
		DisplayName = Sprite,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = xuebi,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = xuebi,
		WorldStaticModel = xuebi,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Sprite:1.0,
            		}
       		}
	}

	item Fenda_Orange	/* 芬达橙子味 */
	{
		DisplayName = Fenda Orange,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = fendaczw,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = fendaczw,
		WorldStaticModel = fendaczw,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = FendaOrange:1.0,
            		}
       		}
	}

	item Fenda_honeypeach	/* 芬达水蜜桃味 */
	{
		DisplayName = Fenda honeypeach,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = fendasmtw,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = fendasmtw,
		WorldStaticModel = fendasmtw,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Fendahoneypeach:1.0,
            		}
       		}
	}

	item Fenda_watermelon	/* 芬达西瓜味 */
	{
		DisplayName = Fenda watermelon,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.25,
        Icon     = fendaxgw,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = fendaxgw,
		WorldStaticModel = fendaxgw,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Fendawatermelon:1.0,
            		}
       		}
	}

	item RedBull	/* 罐装红牛 */
	{
		DisplayName = RedBull,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.15,
        Icon     = hongniu,
		EatType = popcan,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = hongniu,
		WorldStaticModel = hongniu,
		Tooltip = Tooltip_RedBull-p20-j12.5-k25,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
			ContainerName   = CanPop,
			PickRandomFluid	= true,
		    Opened	= false,
			capacity        = 0.25,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = RedBull:1.0,
            		}
       		}
	}

	item Dongpeng_Drink	/* 东鹏特饮 */
	{
		DisplayName = Dongpeng Drink,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = dpty,
		EatType = popcan,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = dpty,
		WorldStaticModel = dpty,
		Tooltip = Tooltip_dongpeng-p20-j12-k36,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 0.3,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Dongpeng:1.0,
            		}
       		}
	}

	item wangzai_Drink	/* 旺仔牛奶 */
	{
		DisplayName = wangzai Drink,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.15,
        Icon     = wznn,
		EatType = popcan,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = wangzai,
		WorldStaticModel = wangzai,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
			ContainerName   = CanPop,
			PickRandomFluid	= true,
			capacity        = 0.25,
			Opened	= false,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = wangzai:1.0,
            		}
       		}
	}

	item Doping_potion	/* 兴奋剂蓝药水 */
	{
		DisplayName = Doping potion,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.15,
        Icon     = xingfengji,
		EatType = popcan,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = xingfengji,
		WorldStaticModel = xingfengji,
		Tooltip = Tooltip_Using it can greatly reduce the pain of characters and restore some physical strength,
		Tags = Cookable;SealedBeverageCan,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
		    Opened	= false,
			capacity        = 0.2,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Dopingpotion:1.0,
            		}
       		}
	}

}