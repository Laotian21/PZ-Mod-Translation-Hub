module Chinesecuisine
{

		/****************************** 调料品物品. ******************************/

	item peanut_oil	/* 花生油 */
	{
		DisplayName = peanut oil,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.3,
        Icon     = hsy,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = hsy,
		WorldStaticModel = hsy,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottlePlastic,
			PickRandomFluid	= true,
			capacity        = 2.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = peanutoil:1.0,
            		}
       		}
	}

	item Yellow_Wine	/* 黄酒 */
	{
		DisplayName = Yellow Wine,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = huangjiu,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = huangjiu,
		WorldStaticModel = huangjiu,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = YellowWine:1.0,
            		}
       		}
	}

	item thinsoy_sauce	/* 味极鲜生抽 */
	{
		DisplayName = thinsoy sauce,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = wjxsc,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = wjxsc,
		WorldStaticModel = wjxsc,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = thinsoysauce:1.0,
            		}
       		}
	}

	item thicksoy_sauce	/* 海天酱油老抽 */
	{
		DisplayName = thicksoy sauce,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = htjylc,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = htjylc,
		WorldStaticModel = htjylc,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = thicksoysauce:1.0,
            		}
       		}
	}

	item cooking_wine	/* 葱姜料酒 */
	{
		DisplayName = cooking wine,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = cjlj,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = cjlj,
		WorldStaticModel = cjlj,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = cookingwine:1.0,
            		}
       		}
	}

	item aromatic_vinegar	/* 香醋 */
	{
		DisplayName = aromatic vinegar,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = xc,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = xc,
		WorldStaticModel = xc,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = aromaticvinegar:1.0,
            		}
       		}
	}

	item mature_vinegar	/* 陈醋 */
	{
		DisplayName = mature vinegar,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.2,
        Icon     = cc,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = cc,
		WorldStaticModel = cc,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 1.0,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = maturevinegar:1.0,
            		}
       		}
	}

	item Oyster_sauce	/* 海天蚝油 */
	{
		DisplayName = Oyster sauce,
		DisplayCategory = Food,
		Type = Normal,
		Weight = 0.1,
        Icon     = hy,
		Packaged = TRUE,
		FillFromDispenserSound = GetWaterFromDispenserPlastic,
		FillFromLakeSound = GetWaterFromLakeBottle,
		FillFromTapSound = GetWaterFromTapPlasticBig,
		FillFromToiletSound = GetWaterFromToilet,
		StaticModel = hy,
		WorldStaticModel = hy,
		Tags = CookableMicrowave,

        	component FluidContainer
        	{
			ContainerName   = BottleGlass,
			PickRandomFluid	= true,
			capacity        = 0.5,
			CustomDrinkSound = DrinkingFromBottlePlastic,

            		Fluids
            		{
                		fluid           = Oystersauce:1.0,
            		}
       		}
	}

}